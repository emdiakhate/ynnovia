<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # If the requested resource is not a real file or directory, serve index.html
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ /index.html [QSA,L]
</IfModule>

# Prevent content negotiation issues
Options -MultiViews
